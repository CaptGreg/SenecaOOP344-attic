#include <iostream>
using namespace std;

int main(int argc, char ** argv)
{
	cout << "a = " << (int) 'a' << endl;
	cout << "b = " << (int) 'b' << endl;

	cout << "'a'+1=" << 'a' + 1 << endl;
	cout << "'a'+'b'=" << 'a' + 'b' << endl;
}
